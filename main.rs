// Hello World program in Rust
// 
// To run this program:
// 1. Ensure you have Rust installed (https://rustup.rs/)
// 2. Compile: rustc main.rs
// 3. Run: ./main (on Unix/Linux/macOS) or main.exe (on Windows)
// 
// Alternatively, you can run directly with: rust-script main.rs
// or if this is part of a Cargo project: cargo run

/// The main function is the entry point of every Rust program.
/// This function prints "Hello, World!" to standard output.
fn main() {
    // Print the required message to stdout
    // The println! macro adds a newline character at the end
    println!("Hello, World!");
}